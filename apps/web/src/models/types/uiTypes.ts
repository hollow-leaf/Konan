import React from "react";

export type NavbarItemType = {
  name: string;
  icon: React.ReactElement;
  href: string;
  target: string;
};

export type ImageType = {
  label: string;
  name: string;
  title: string;
  linkedin: string;
};
